import 'package:flutter/material.dart';
class FavoriteScreen extends StatelessWidget {
  static const screenRout = '/Favorite';

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("as"),);
  }
}
